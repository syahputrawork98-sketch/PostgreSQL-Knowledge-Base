# Contoh Command Database Di psql

Command yang sering dipakai pemula:

```text
\l
\c learning_db
```

Keterangan singkat:

- `\l` menampilkan daftar database
- `\c learning_db` berpindah koneksi ke database `learning_db`
