# Contoh Struktur Object

Contoh hirarki sederhana:

```text
PostgreSQL Server
`-- school_db
    `-- public
        `-- students
```

Keterangan:

- `school_db` adalah database
- `public` adalah schema
- `students` adalah table
